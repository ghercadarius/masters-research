apiVersion: v1
kind: Service
metadata:
  name: __APP_NAME__
  namespace: __NAMESPACE__
spec:
  type: LoadBalancer
  selector:
    app: __APP_NAME__
  ports:
  - name: http
    protocol: TCP
    port: __SERVICE_PORT__
    targetPort: __APP_PORT__
