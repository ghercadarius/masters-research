apiVersion: v1
kind: Service
metadata:
  name: sample-python-webapp
  namespace: sku-bench
spec:
  type: LoadBalancer
  selector:
    app: sample-python-webapp
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 5000
